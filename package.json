{
  "devDependencies": {
    "prettier": "^2.7.1",
    "prettier-plugin-astro": "^0.5.0",
    "prettier-plugin-tailwindcss": "0.1.13",
    "turbo": "^1.4.3"
  },
  "scripts": {
    "build": "turbo run build",
    "build:ae": "turbo run api-extractor -- --local",
    "test": "turbo run test",
    "lint": "turbo run lint"
  }
}